package Stacks_And_Queues.Additional_Arrays.ArrayPrint;

public class PrintArray_Strings {
    public PrintArray_Strings(String[] strings) {
        for (int j = 0; j < strings.length; j++) {
            System.out.print(strings[j] + " ");
        }
        System.out.println();
    }
}
